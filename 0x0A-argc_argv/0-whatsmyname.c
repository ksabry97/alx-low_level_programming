#include <stdio.h>
/**
 * main -  return the name of the program
 * @argc : integer
 * @argv : array of string
 *
 * Return: always (0)
 **/
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
